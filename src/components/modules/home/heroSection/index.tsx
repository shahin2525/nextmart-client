import style from "./heroSection.module.css";
const HeroSection = () => {
  return (
    <div className={`${style.banner} container mx-auto`}>
      <h1>Hero section 1</h1>
    </div>
  );
};

export default HeroSection;
